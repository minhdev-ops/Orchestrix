# Kế hoạch: Test cấp 3 (toàn stack Laravel → FastAPI) + migrate DB

## Tổng quan pipeline test
```
curl  ──>  Laravel :8001  ──auth──>  POST /api/plant-doctor/diagnose
                                            │
                                            ▼
                          AIPlantDoctorService (provider=vit_local)
                                            │
                                            ▼  HTTP POST /predict
                          FastAPI ai_service :8501  ──>  best_vit.keras
                                            │
                                            ▼
                          INSERT vào plant_diagnoses (MySQL)
```

## Các phát hiện quan trọng (đã khảo sát)

1. **Bảng `plant_diagnoses` ĐÃ TỒN TẠI** từ migration `2026_06_13_192101_create_plant_diagnoses_table.php` (đã ran, schema đẹp: enum severity, default provider='gemini', uuid unique). → **Phải XÓA migration trùng `2026_07_27_...` tôi tạo**, nếu không `php artisan migrate` sẽ lỗi "table already exists".
2. **Passport keys CHƯA có** (`storage/oauth-private.key`, `oauth-public.key` không tồn tại) → `/api/login` sẽ 500. Cần `php artisan passport:keys`.
3. **5 OAuth migrations Pending là DUPLICATE** (bảng đã tạo từ batch `2026_05_14_*`) → nếu chạy `php artisan migrate` sẽ fail. Cần xử lý bằng cách đánh dấu đã-run (`migrate:status` rồi bỏ qua, hoặc insert thủ công vào bảng `migrations`).
4. **Port 8000 đang bị AquaVio chiếm** → chạy Orchestrix ở `--port=8001`.
5. Endpoint `POST /api/plant-doctor/diagnose` CHỈ cần Bearer token (không cần permission role).

## Các bước thực hiện

### Bước 1 — Dọn migration trùng (bắt buộc, để migrate không lỗi)
- Xóa file `app/Modules/AgriVerse/Database/Migrations/2026_07_27_000001_create_plant_diagnoses_table.php` (trùng bảng với `2026_06_13_*`).
- Lý do: schema cũ (`2026_06_13`) còn đầy đủ hơn (enum severity) và đã chạy rồi.

### Bước 2 — Xử lý 5 OAuth migrations Pending (đánh dấu đã chạy)
- Kiểm tra `php artisan migrate:status`, xác nhận 5 migration `2026_05_17_15582*_create_oauth_*_table` đang Pending nhưng bảng thật đã tồn tại (từ batch `2026_05_14_*`).
- Đánh dấu chúng đã-run bằng INSERT trực tiếp vào bảng `migrations` (theo đúng filename), để `php artisan migrate` không cố chạy lại.
- Verify: `php artisan migrate:status` không còn Pending nào liên quan OAuth.

### Bước 3 — Sinh Passport keys
- `php artisan passport:keys` (tạo `storage/oauth-private.key` + `oauth-public.key`).
- Verify file xuất hiện, `php artisan passport:client --personal` nếu cần personal access client (Passport 13 có thể tự có).

### Bước 4 — Đảm bảo có user để login
- Kiểm tra bảng `users`: có user `is_active=1` không (admin@orchestrix.com / 12345678 là pattern trong `patch_admin.cjs`).
- Nếu chưa có → tạo bằng tinker/seed: `User::create([...])` + assign role, hoặc dùng endpoint `POST /api/register` nếu open.
- Verify: `SELECT id,email,is_active FROM users LIMIT 5`.

### Bước 5 — Cấu hình `.env` cho vit_local
- Đảm bảo trong `.env`:
  - `AI_PLANT_DOCTOR_PROVIDER=vit_local`
  - `AI_PLANT_DOCTOR_VIT_SERVICE_URL=http://localhost:8501` (local; nếu chạy Laravel trong docker thì `http://ai_service:8501`)
  - `AI_PLANT_DOCTOR_VIT_TOKEN=` (rỗng)
  - `AI_PLANT_DOCTOR_INFO_PROVIDER=` (rỗng → tránh phụ thuộc Gemini API key khi test; service vẫn trả kết quả ViT + template description)
- Clear config cache: `php artisan config:clear`.

### Bước 6 — Khởi động ai_service (Python FastAPI)
- Background: `cd ai_service && AI_MODEL_PATH=../best_vit.keras AI_LABELS_PATH=labels.txt python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8501 --log-level warning`
- Poll `GET /health` đến khi `model_loaded=true` (~5-10s).

### Bước 7 — Khởi động Laravel server
- Background: `cd /home/couterit/Work/02.Study/Project/Orchestrix && php artisan serve --host=127.0.0.1 --port=8001`
- Verify: `curl http://127.0.0.1:8001/up` → 200.

### Bước 8 — Login lấy Passport token
- `POST http://127.0.0.1:8001/api/login` body `email` + `password`.
- Trích `data.token.accessToken` (hoặc `access_token`) từ response → `$TOKEN`.
- Nếu login fail do user chưa active → quay Bước 4.

### Bước 9 — Gọi endpoint diagnose (test chính)
```bash
curl -X POST http://127.0.0.1:8001/api/plant-doctor/diagnose \
     -H "Authorization: Bearer $TOKEN" \
     -F "image=@PlantDiseaseDataset/plant_disease_dataset/val/Powdery Mildew (Cotton)/Powdery Mildew (Cotton)_2.jpg" \
     -F "symptoms=White powdery spots on leaves"
```
- Kỳ vọng response JSON có: `diagnosis.disease_name`, `confidence`, `provider='vit_local'` (hoặc `'vit_local'` vì info_provider rỗng), `treatments`, `prevention`.

### Bước 10 — Verify DB row đã insert
- `SELECT id,uuid,plant_name,disease_name,confidence,severity,provider,created_at FROM plant_diagnoses ORDER BY id DESC LIMIT 1;`
- Kỳ vọng: 1 row mới, `provider='vit_local'`, `disease_name` hợp lệ.

### Bước 11 — Dọn dẹp
- Kill 2 background process (php serve + uvicorn).
- Giữ nguyên cấu hình `.env` (vit_local) để bạn dùng tiếp, hoặc revert nếu muốn.

## Kết quả bạn nhận được
- Endpoint `/api/plant-doctor/diagnose` chạy end-to-end với model local `best_vit.keras`.
- 1 row mẫu trong bảng `plant_diagnoses`.
- Migration trùng được dọn (tránh lỗi tương lai).
- Passport keys + OAuth migration state sạch.

## Rủi ro & cách xử lý
- **Nếu login vẫn 500** sau khi có keys → có thể thiếu Passport personal access client → chạy `php artisan passport:client --personal` (interactive, sẽ hỏi name/redirect).
- **Nếu `info_provider=gemini`** mà không có API key → service vẫn trả kết quả ViT nhưng description rỗng. Mặc định tôi set `info_provider=` (rỗng) khi test để không phụ thuộc cloud.
- **Nếu user chưa active** → activate bằng `UPDATE users SET is_active=1, email_verified_at=NOW() WHERE email='...'`.